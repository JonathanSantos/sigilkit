{
  "name": "@sigil/cli",
  "version": "0.1.0",
  "description": "CLI do sigil — orquestração e IO. build/check/dev.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "sigil": "./bin/sigil.js"
  },
  "files": ["dist", "bin"],
  "scripts": {
    "build": "tsc -b ."
  },
  "dependencies": {
    "@sigil/compiler": "0.1.0",
    "@sigil/test": "0.1.0",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3"
  }
}

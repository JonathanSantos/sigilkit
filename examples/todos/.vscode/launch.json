{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run todos (sigil example)",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"]
    }
  ]
}
